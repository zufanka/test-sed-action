name: clean-newlines
on: push
jobs:
  clean-carriage-returns:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: |
          sed $'s/cupcake/BLOOD/' -i $GITHUB_REPOSITORY/random/*.txt
